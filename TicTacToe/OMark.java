import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class OMark here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class OMark extends Actor
{
    public OMark(){
        GreenfootImage image = getImage();
        image.scale(135, 135);
        setImage(image);
    }
}
